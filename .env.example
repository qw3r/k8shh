# This tool reads your default kubeconfig (same as kubectl); no env vars are required.
#
# Optionally point at a specific kubeconfig. Supports a colon-separated list of
# files, which are merged — identical semantics to kubectl's $KUBECONFIG.
# KUBECONFIG=/path/to/kubeconfig:/path/to/another
